map $ssl_server_name $tls_certificate {
    default /etc/nginx/tls/tls.crt;
}

map $ssl_server_name $tls_certificate_key {
    default /etc/nginx/tls/tls.key;
}

server {
    listen 8080;
    listen 8443 ssl;
    server_name student-service.internal;

    ssl_certificate $tls_certificate;
    ssl_certificate_key $tls_certificate_key;
    ssl_protocols TLSv1.2 TLSv1.3;

    root /usr/share/nginx/html;
    index index.html;

    location = /healthz {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location /api/ {
        proxy_pass http://student-service-backend.student-service.svc.cluster.local:8000;
        proxy_set_header Host student-service-backend.student-service.svc.cluster.local;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
