# Student Service Backend

This is the Django API. Its Kubernetes manifests are in `apps/student-service/`.

## Local development

```bash
mise run backend:sync
mise run backend:check
mise run backend:run
```
