# Networking

Private networking for the LAN:

- `coredns/`: internal DNS names.
- `cilium-l2/`: LAN addresses for services.
- `gateway-api/`: the shared Gateway.

The shared Gateway uses `192.168.1.240`. Harbor uses `192.168.1.241`.
